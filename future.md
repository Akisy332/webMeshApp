# 🏗️ Полная архитектура проекта WebMeshApp

## 📁 **Структура проекта**

```
src/
├── features/                          # ФИЧИ (бизнес-логика)
│   ├── telemetry/                    # Телеметрия и данные модулей
│   ├── session/                      # Управление сессиями
│   ├── auth/                         # Аутентификация
│   ├── map/                          # Карта и визуализация
│   └── [future-features]/            # Будущие фичи
│
├── shared/                           # ПЕРЕИСПОЛЬЗУЕМЫЕ компоненты
│   ├── components/                   # UI компоненты
│   ├── services/                     # Общие сервисы
│   └── types/                        # Общие типы
│
├── core/                             # ЯДРО приложения
│   ├── event-bus.ts                  # Центральная шина событий
│   ├── component-base.ts             # Базовый класс компонентов
│   ├── constants.ts                  # Константы и настройки
│   ├── socket-service.ts             # WebSocket соединение
│   ├── api/                          # API клиенты (опционально)
│   └── types.ts                      # Базовые типы
│
└── app/                              # КОРНЕВОЙ модуль
    ├── main.ts                       # Точка входа
    ├── app.component.ts              # Главный компонент
    └── templates/                    # HTML шаблоны
```

## 🎯 **Правила архитектуры**

### **1. Разделение ответственности**

```
Компоненты    → Отображение + пользовательские взаимодействия
Сервисы фич   → Бизнес-логика + работа с данными
Utils         → Логика отображения + форматирование
API слой      → Чистые HTTP запросы
```

### **2. Модульность фич**

Каждая фича в `features/` должна быть САМОДОСТАТОЧНОЙ:

```
features/telemetry/
├── components/     # Компоненты ТОЛЬКО для телеметрии
├── services/       # Сервисы ТОЛЬКО для телеметрии
├── types/          # Типы ТОЛЬКО для телеметрии
└── utils/          # Утилиты ТОЛЬКО для телеметрии
```

### **3. Коммуникация между фичами**

-   ✅ **Через EventBus** - для слабой связности
-   ✅ **Через сервисы** - если нужно прямое взаимодействие
-   ❌ **Прямые импорты компонентов** - запрещено

## 🔧 **Организация кода**

### **Компоненты (ВСЕГДА в папках)**

```
features/telemetry/components/session-data-table/
├── session-data-table.component.ts     # Основной компонент (150-200 строк)
├── session-data-table.styles.css       # Стили компонента
├── session-data-table.types.ts         # Типы компонента (если специфичные)
└── utils/                              # Утилиты ТОЛЬКО для этого компонента
    ├── table-render.utils.ts           # Логика рендеринга
    ├── status.utils.ts                 # Логика статусов
    └── time.utils.ts                   # Логика времени
```

**Правило:** Если файл используется только в одном компоненте - он остается в его `utils/`

### **Сервисы фич**

```typescript
// features/session/services/session.service.ts
export class SessionService {
    // ✅ Бизнес-логика для ВСЕХ компонентов фичи
    // ✅ Работа с данными
    // ✅ Может использовать API слой
    // ✅ НЕ знает о DOM/UI
}
```

**Правило:** Сервис создается когда логика используется в 2+ компонентах фичи

### **API вызовы**

```typescript
// ТОЛЬКО в services/ или core/api/
export class SessionService {
    async getSessions(): Promise<Session[]> {
        // ✅ Разрешено здесь
        return await fetch('/api/sessions');
    }
}

// ❌ ЗАПРЕЩЕНО в компонентах, утилитах, shared/
```

## 🔄 **Взаимодействие и данные**

### **Flow данных**

```
Бэкенд → SocketService → EventBus → Компоненты → Сервисы → API → Бэкенд
```

### **Пример: Новые данные модулей**

```typescript
// 1. SocketService получает данные
socket.on('moduleUpdate', (data) => {
    eventBus.emit(EventTypes.SOCKET.NEW_DATA_MODULE, data);
});

// 2. Компонент слушает
class SessionDataTableComponent {
    constructor() {
        eventBus.on(EventTypes.SOCKET.NEW_DATA_MODULE, (data) => {
            this.handleNewData(data);
        });
    }
}

// 3. Сервис для сложной обработки
class ModuleDataService {
    processModuleData(rawData: any): ModuleData[] {
        // Бизнес-логика обработки
    }
}
```

## 🛡️ **Защитные правила**

### **Строгие запреты**

1. ❌ **Прямые API вызовы** outside services/
2. ❌ **Импорты между фичами** на уровне компонентов
3. ❌ **Бизнес-логика в shared/components**
4. ❌ **Большие файлы** (>300 строк) - разбивать на утилиты
5. ❌ **Знание о других фичах** внутри компонентов

### **Разрешенные взаимодействия**

1. ✅ **EventBus** - для событий между фичами
2. ✅ **Сервисы фич** - могут использовать друг друга
3. ✅ **Shared компоненты** - только переиспользуемый UI
4. ✅ **Core утилиты** - могут использовать все

## 🚀 **Практические примеры**

### **Правильно:**

```typescript
// Компонент делегирует сервису
class SessionDropdownComponent {
    async loadSessions() {
        this.sessions = await this.sessionService.getSessions(); // ✅
    }
}

// Сервис содержит бизнес-логику
class SessionService {
    async getSessions() {
        const sessions = await this.apiService.getSessions();
        return this.sortByDate(sessions); // ✅ Бизнес-логика
    }
}
```

### **Неправильно:**

```typescript
// Компонент делает API вызов
class SessionDropdownComponent {
    async loadSessions() {
        this.sessions = await fetch('/api/sessions'); // ❌
    }
}

// Утилита содержит бизнес-логику
export function calculateSessionStats() {
    fetch('/api/sessions/stats'); // ❌
}
```

## 💡 **Особенности для твоего проекта**

### **MapService архитектура**

```
features/map/
├── services/
│   ├── map-core.service.ts           # Тонкий координатор
│   ├── marker.service.ts             # Логика маркеров
│   ├── trace.service.ts              # Логика трасс
│   └── layer.service.ts              # Логика слоев
├── plugins/                          # Инструменты карты
│   ├── time-slider/                  # Временной слайдер
│   ├── measurement/                  # Линейка измерений
│   └── context-menu/                 # Контекстное меню
└── components/
    └── map/                          # Основной компонент карты
```

### **Mesh-анализ (будущее)**

```typescript
// features/telemetry/services/mesh-analysis.service.ts
// Будет использоваться в таблице, карте и панели здоровья сети
```

## 🔄 **Миграция с текущего кода**

### **Фаза 1: Реорганизация**

-   Создать структуру `features/`
-   Перенести компоненты в соответствующие фичи
-   Вынести логику в `utils/`

### **Фаза 2: Сервисный слой**

-   Создать сервисы для фич
-   Вынести API вызовы из компонентов
-   Настроить EventBus коммуникацию

### **Фаза 3: Оптимизация**

-   Создать общий API слой если нужно
-   Добавить dependency injection
-   Оптимизировать взаимодействия

## ✅ **Преимущества этой архитектуры**

1. **Масштабируемость** - легко добавлять новые фичи
2. **Тестируемость** - каждый модуль изолирован
3. **Поддерживаемость** - четкие границы ответственности
4. **Переиспользуемость** - shared компоненты работают везде
5. **Гибкость** - можно менять одну фичу не затрагивая другие

Эта архитектура готова к росту от текущего состояния до сложной системы с mesh-анализом, расширенной картой и множеством инструментов!
